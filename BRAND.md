# BRAND — CWI Attitude Engine

**Attitude Engine: turn any identity's attitude into agent behavior — with bounds that always win.**

For AI developers and agent builders who need machine-readable personality: define who an identity is, how it carries itself, and what it must never do — and get back a behavior card (config JSON + system prompt + non-overridable bounds) any agent runtime can consume. Zero dependencies, pure function, deterministic.

Voice: 2AM at the supervisor's desk — near-black `#0A0C10`, signal amber `#FFB224`, phosphor green `#3DFF88`. Direct, technical, no hype. Claims carry provenance (verified / editorial / assumed); nothing is claimed about endorsements, integrations, users, or adoption.
